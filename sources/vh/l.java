package vh;

import android.graphics.Canvas;
import org.telegram.ui.r4;
public final class l extends r4 {
    @Override
    public final void onDraw(Canvas canvas) {
        if (this.P.getRealCount() > 1) {
            super.onDraw(canvas);
        }
    }
}
